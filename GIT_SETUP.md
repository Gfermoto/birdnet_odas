# Инструкция по отправке проекта в GitHub

## 🔒 Безопасность

**Важно**: Я не буду выполнять команды `git push` напрямую. Вы сделаете это сами, чтобы ваши ключи остались в безопасности.

## 📋 Шаги для отправки в GitHub

### 1. Проверка .gitignore

Убедитесь, что `.gitignore` создан и содержит все необходимые исключения:
```bash
cat .gitignore
```

### 2. Инициализация Git репозитория

```bash
cd /home/gfer/birdnet_odas
git init
```

### 3. Добавление файлов

```bash
# Проверка, что будет добавлено (без секретов)
git add -n .

# Если всё правильно, добавляем файлы
git add .
```

### 4. Первый коммит

```bash
git commit -m "Initial commit: BirdNET-ODAS setup for NanoPi M4B"
```

### 5. Настройка удалённого репозитория

```bash
# Добавить удалённый репозиторий
git remote add origin https://github.com/Gfermoto/birdnet_odas.git

# Или через SSH (если настроен SSH ключ)
# git remote add origin git@github.com:Gfermoto/birdnet_odas.git
```

### 6. Отправка в GitHub

**Вариант A: Через HTTPS (требует токен)**

1. Создайте Personal Access Token на GitHub:
   - Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token (classic)
   - Выберите права: `repo` (полный доступ к репозиториям)
   - Скопируйте токен

2. Выполните push (GitHub попросит ввести токен вместо пароля):
```bash
git branch -M main
git push -u origin main
# При запросе username: введите Gfermoto
# При запросе password: вставьте ваш Personal Access Token
```

**Вариант B: Через SSH (рекомендуется, безопаснее)**

1. Проверьте наличие SSH ключа:
```bash
ls -la ~/.ssh/id_*.pub
```

2. Если ключа нет, создайте:
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
# Нажмите Enter для всех вопросов (или задайте пароль)
```

3. Добавьте публичный ключ в GitHub:
```bash
cat ~/.ssh/id_ed25519.pub
# Скопируйте вывод и добавьте в GitHub:
# Settings → SSH and GPG keys → New SSH key
```

4. Проверьте подключение:
```bash
ssh -T git@github.com
```

5. Если используете SSH, измените remote URL:
```bash
git remote set-url origin git@github.com:Gfermoto/birdnet_odas.git
```

6. Отправьте код:
```bash
git branch -M main
git push -u origin main
```

## ✅ Проверка перед отправкой

Перед `git push` убедитесь, что не отправляются секреты:

```bash
# Проверка, что будет отправлено
git ls-files

# Проверка на наличие секретов в файлах
grep -r "password\|secret\|key\|token" --include="*.md" --include="*.sh" --include="*.py" . || echo "Секретов не найдено"
```

## 🔍 Что защищает .gitignore

Созданный `.gitignore` исключает:
- Файлы с секретами (`.env`, `.key`, `.pem`, `.secret`)
- Логи и временные файлы
- Данные записей (`.wav`, `.mp3`)
- Системные файлы
- Backup файлы

## 🚨 Если случайно отправили секрет

Если вы случайно отправили секрет в GitHub:

1. **Немедленно** удалите секрет из файла
2. Удалите файл из истории Git:
```bash
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch путь/к/файлу" \
  --prune-empty --tag-name-filter cat -- --all
```
3. Принудительно отправьте изменения:
```bash
git push origin --force --all
```
4. **Смените секрет** (пароль, ключ, токен) — он уже скомпрометирован!

## 📝 Дополнительные команды

```bash
# Просмотр статуса
git status

# Просмотр изменений
git diff

# Просмотр истории
git log --oneline

# Отмена последнего коммита (если ещё не отправили)
git reset --soft HEAD~1
```

## 🔗 Полезные ссылки

- [GitHub: Creating a Personal Access Token](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token)
- [GitHub: Adding SSH Keys](https://docs.github.com/en/authentication/connecting-to-github-with-ssh/adding-a-new-ssh-key-to-your-github-account)
- [Git: Removing sensitive data](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/removing-sensitive-data-from-a-repository)

